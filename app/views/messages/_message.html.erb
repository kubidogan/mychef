<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.username %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
  <% if message.photo.attached? %>
    <%= cl_image_tag message.photo.key, class:"message-img"%>
  <% end %>
</div>
